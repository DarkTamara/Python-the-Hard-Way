#making this one line long/shorter
from sys import argv
script, from_file, to_file = argv

indata = open(from_file).read()

out_file=open(to file).write() #writes a file with the info from indata avriable
#no work like this
out_file.close() #closes the file
in_file.close()  #same
# to make it one like simply use ; afer each of them and
#write it on one line like a bastard
